파이썬 __name__ 과 __main__ 에 대한 설명 

__name__은 모듈의 이름은 두가지다. 직접 실행되는 모듈은 main이 입력되고,         모듈이란 파이썬 파일이름이다.
직접 실행되지 않는 (import)된 모듈은 그 파일의 이름이 입력된다.

즉, 이 글의 목적인 __name__ == "__main__"이 뭐냐는 의문점에 대해서 설명하면
이거는 모듈이 직접 실행되는 경우에는 if문 안의 코드가 실행되는 의미입니다.

